<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Plain brand-orange field, no eye. The mandatory OS launch frame is just
         the background; the Flutter splash draws and animates the eyes so the
         custom splash is effectively the only screen the user sees. -->
    <item android:drawable="@color/splash_background" />
</layer-list>
